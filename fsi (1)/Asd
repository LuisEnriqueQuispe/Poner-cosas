<?php require 'conexion_postgres.php'; session_start(); $usuario=$_POST['user']; $clave=$_POST['pass']; $query="SELECT * FROM ucsm WHERE usuario = '$usuario' AND contraseña = '$clave'"; $consulta=pg_query($conexion,$query); $cantidad=pg_num_rows($consulta); if($cantidad>0) { $_SESSION['nombre_usuario']=$usuario; header("location: ingreso.php"); }else{ echo "Alguno de los datos ingresados es incorrecto!"; } ?>
